site :opscode

metadata
cookbook 'locale', github: "BCS-io/chef-locale"
cookbook 'locale-wrapper', github: "BCS-io/chef-locale-wrapper"
cookbook 'postgresql', github: "phlipper/chef-postgresql"

group :integration do
  cookbook 'apt', '~> 2.0'
end